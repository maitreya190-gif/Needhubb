{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && pnpm install --frozen-lockfile && pnpm exec prisma generate --schema=apps/api/prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "cd ../.. && pnpm exec tsx apps/api/src/index.ts",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
