[phases.setup]
nixPkgs = ["nodejs_22", "pnpm-9_x", "openssl"]

[phases.install]
cmds = ["pnpm install --frozen-lockfile"]

[phases.build]
cmds = [
  "pnpm --filter @needhub/api exec prisma generate",
  "pnpm --filter @needhub/api build"
]

[start]
cmd = "pnpm --filter @needhub/api start"
